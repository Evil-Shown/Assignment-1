{
  "name": "assignment-1",
  "version": "1.0.0",
  "description": "Playwright test automation for Singlish to Sinhala Translator",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "report": "npx playwright show-report",
    "install:browsers": "npx playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.1",
    "@types/node": "^25.1.0"
  }
}
